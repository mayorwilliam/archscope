"""Task queue helpers for the worker module.

This second paragraph must not appear in the summary.
"""


def enqueue(task):
    """Push a task onto the queue."""
    return task


class Scheduler:
    '''Runs periodic jobs
    on a fixed interval.

    Second paragraph stays out.
    '''

    def run(self):
        """Method docstrings stay out — only top-level symbols carry docs."""


def plain():
    return None
