/** Placeholder — the timeline lands in phase 3 of the wiki plan. */
export function TimelineScreen() {
  return (
    <div className="view">
      <div className="stub-note" data-testid="timeline-stub">
        The timeline is coming next: tags as milestones, recent commits as detail,
        <br />
        two-point compare on the existing diff overlay.
      </div>
    </div>
  );
}
